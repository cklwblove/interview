/**
 *
 * @authors liwb (you@example.org)
 * @date    2018/5/8 上午10:31
 * @version $ 作为连接Observer和Compile的桥梁，能够订阅并收到每个属性变动的通知，执行指令绑定的相应回调函数，从而更新视图
 */

/* name module */

(function ($, w, d) {

    'use strict';

    /* do something */

}(jQuery, window, document));
